Installation of google-chrome-stable and python3 are required.

Default action must redirect to a pager like `less`.
later on ag-grid should take place of dbgate

-r --random
-a --analyze
-s --scrape
-R --revert # reverts just one revision back by default can take arguments
-b --backup
-g --good
-C --clean # delete rows that are not full
-c --clean # export then confirm export and then delete rows that are not full
-i --input
