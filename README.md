Daemon mode and sending notifications once an hour.
Might require async. Although might not need it.
Wikipedia page link it at the bottom it does not require module.

python3 -m pip install PySide6
sudo apt install qt6-tools-dev-tools
designer6
Using of pyinstaller?
